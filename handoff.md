# Handoff — VisionStudio

Coordination doc for multiple concurrent Claude Code sessions working this repo. **Read this before making changes, and update the relevant section when you finish a unit of work** — this repo has had unintentional overwrites and stale-state confusion from sessions working blind to each other.

Last updated: 2026-09-03 (fixed the Cartoonize preset — backgrounds weren't cartoonizing, PR #17 — then re-synced the Archon board to pick up #16/#17), by session `01VWQ2iPrjx7eUkMxbcZZdt5`.

## 🎨 Cartoonize preset fixed — background wasn't cartoonizing (PR #17)

User reported: subject cartoonizes great, background still looks like a photo, worse than it used to be months ago. Checked `git log -S` on the prompt string — it hasn't changed since the initial commit, so this is Gemini model behavior drift on Google's side, not a regression here. Fixed by making the prompt explicit that the *entire* image (including background) should get the same flat cartoon shading and bold outlines, rather than leaving that to the model's judgment.

**Verified with a real side-by-side comparison, not just a plausible-sounding wording tweak**: generated a realistic test photo via the Gemini API directly (a dog on a porch with a detailed house/trees background — no real user photo used), then ran both the old and new prompt against the same source image. Old prompt reproduced the exact reported symptom (nicely cartoonized dog, photorealistic house/porch/trees). New prompt produced visibly more consistent cartoon styling across trees, sky, and house. Board re-synced afterward to add tasks for this PR and #16 (persistent selected-indicator on preset buttons, merged just before it) — both were missing from the backfill done earlier today since they landed after that sync ran.

## 📋 Archon board synced 2026-09-03

Project `bef7e278-bd9d-4c44-905d-5d7c30147a16` had only 3 tasks (all `done`, all pre-dating the day's later PRs). Backfilled one `done` task per merged PR (#7–#15, plus two direct-to-main commits and the server-side nginx fix) so the board matches shipped reality — 13 tasks total now, 12 `done` / 1 `todo`. Note: `manage_task`'s `create` action ignores a passed `status` — every task lands as `todo` and needs a follow-up `update` call to set the real status; don't assume `create(..., status: "done")` worked without checking.

**One real gap found while updating docs, not previously tracked**: `index.html`'s favicon `<link>` still points at `Digital_Eye_icon.png`, deleted by the rebrand (`1b06c98`) — **the favicon is currently broken (404s)**. Not fixed this session (docs-only scope was asked for) — tracked as its own `todo` task on the board ("Fix broken favicon"), and noted on the rebrand task's description too since that's the work that caused the gap.

## ✅ All platforms confirmed working by the user — 2026-09-03

After the chain of fixes below (nginx body limit, stale localStorage, shared API-key state — PR #15 — plus PR #14's LAN-access fix from a separate session), the user confirmed: the native iOS app, the native macOS app, and the web app (loaded from both the MacBook and the iPhone's browser) all work correctly. This closes out the "Could not reach backend" / "API key required" saga that spanned most of this session — **the actual bugs were the nginx body limit and the duplicate `useApiKey()` hook instances, not anything about the secret, CORS, TLS, or DNS**, all of which were red herrings that looked identical to the user because WKWebView reports every one of those failure modes as the same generic "Load failed" / "API key required" text.

## 🐛 Hosted backend rejected any real photo upload with 413 — FIXED 2026-09-03 (server config, not a commit)

The user kept hitting `Could not reach backend at https://vision.th3rdai.com - Load failed` on the real iOS app even after: rebuilding with the correct `VITE_APP_SECRET`, confirming CORS/TLS/ATS were all fine via `nscurl --ats-diagnostics` and manual `curl` tests, and a full delete+reinstall to rule out stale `localStorage` (a real secondary issue that was also present and also fixed, see below). None of that was the actual bug.

**Root cause**: `/etc/nginx/sites-enabled/vision.th3rdai.com` had no `client_max_body_size` directive, so nginx used its default **1MB** limit on `location /`. `backend/index.js` sets Express's own body limit to 25MB and the app resizes images up to 4MB before sending — any real photo's base64-encoded JSON payload blows past 1MB immediately. Nginx returns `413 Request Entity Too Large` with `Connection: close` for an oversized body — reproduced directly with `curl` (`-d @<2MB-json-file>`), confirmed `413`. **WKWebView's `fetch()` reports this as a generic "Load failed" network error, not a readable HTTP status** — that's why it looked identical to a DNS/CORS/secret failure from the app's side, and why `GET /api/key-status` (tiny, no body) always worked fine while every real edit/generate request silently failed. This also explains why the request never showed up in `journalctl` — nginx rejects it before proxying to the Node app at all, so the application-level logs are clean regardless.

**Fix**: added `client_max_body_size 25m;` inside the `location /` block (matching Express's own limit), `nginx -t` to validate, `systemctl reload nginx` (no downtime). **Gotcha hit while doing this**: the first attempt to back up the config file copied it to `/etc/nginx/sites-enabled/vision.th3rdai.com.bak-<timestamp>` — nginx loads *every* file in `sites-enabled/` regardless of name, so this created a duplicate `server_name vision.th3rdai.com` block and `nginx -t` warned "conflicting server name" (harmless per se, `nginx -t` still passed, but sloppy and worth avoiding) — moved the backup to `/root/nginx-backups/` instead, warning gone. **If you ever back up an nginx site config, back it up outside `sites-enabled/`/`sites-available/`, not alongside the live file.**

**Verified**: reproduced the `413` with a raw 2MB `curl` POST before the fix, confirmed a `200`/`400` (proper JSON, past nginx) after — and confirmed via `journalctl` that the request now actually reaches the Node app (it didn't before, silently).

**Secondary issue found and fixed along the way — stale `localStorage`**: `src/backendUrl.ts`'s `getBackendUrl()` checks a `localStorage`-saved `backend_url` value before the build-time `VITE_BACKEND_URL` default. The user's iPhone had `http://localhost:3001` saved there from early in this project's history (likely a leftover `AppSettings.tsx` write from before that UI was removed) — this persists across app *updates* (reinstalling the same bundle ID keeps app data) but not a full **delete**. This was real and did need fixing (a fresh install without deleting the old app first would have kept hitting `localhost:3001` regardless of what the new build's default was), but **it was not the reason the error persisted after the delete+reinstall** — the nginx 1MB limit was. Both bugs were stacked on top of each other; only fixing both together resolves the user's actual symptom.

## 🐛 Web app broken when accessed from another device on the LAN — FIXED 2026-09-03 (PR #14, `2c9f783`)

User reported: opening the *web app* (browser, not the native iOS/Electron apps) on their phone — loading the dev frontend at `http://192.168.50.7:3002`, since Vite's dev server binds `--host=0.0.0.0` — failed to process images. Two separate bugs, both needed fixing for LAN browser access to work at all:

- **`src/backendUrl.ts`'s dev fallback was a hardcoded `http://localhost:3001`** baked into the bundle. On a phone's browser, `localhost` means the phone itself, not the Mac serving the page — so every backend call went to nowhere. Fixed: now derived from `window.location.hostname` at runtime (falls back to the literal string `'localhost'` only in non-browser contexts like tests/SSR, where `window` is undefined).
- **`backend/index.js`'s CORS only allowed the exact configured `FRONTEND_ORIGIN`** (`http://localhost:3002` per `startup.sh`'s local-dev default). A phone's browser sends `Origin: http://192.168.x.x:3002`, which never matched — even after fixing the URL, the backend would've still rejected the request. Fixed: added a `LAN_ORIGIN_PATTERN` regex allowance scoped to RFC1918 private ranges (`10.x`, `172.16-31.x`, `192.168.x`) on the same port the dev frontend runs on — **only active when `HOSTED` is unset**, so a production deployment (`HOSTED=true`, e.g. `vision.th3rdai.com`) stays locked to exactly its configured origin, completely unaffected by this change.

**Verified**: regex tested against public IPs / wrong ports / edge-of-range RFC1918 addresses (all correctly pass/fail), then a real running backend confirmed via `curl` — a LAN-origin request now gets a proper `Access-Control-Allow-Origin` response, the same request against a `HOSTED=true` instance is still correctly rejected, and a request to the frontend via the LAN IP loads successfully. Restarted the user's dev servers (`./startup.sh restart`, with explicit confirmation first since it's their live session) so the fix is live — **the running dev servers on this Mac already reflect this fix**, confirmed by diffing the running `backend/index.js` against the merged commit.

**This was a pre-existing gap, not a regression from recent hardening/geo-block work** — the hardcoded `localhost` and the fixed-origin CORS check both predate this session's other changes; it just hadn't been exercised by a LAN-browser client before (prior testing used the native iOS app, which bakes in `VITE_BACKEND_URL` explicitly, or the Mac's own browser at `localhost`).

## ⚠️ The physical iPhone is shared state too, not just the git checkout

While PR #13 was still open, `01PbFrBceHHpLS3N8FhU8pqQ` rebuilt and reinstalled the app on the user's real iPhone **twice** for two unrelated `main`-branch fixes (an uploaded-photo color bug, an iOS text-input auto-zoom bug). Both rebuilds used `VITE_BACKEND_URL=http://192.168.50.7:3001` (the LAN-dev default, correct at the time since neither PR #11 nor #13 had merged in that session's view) with no `VITE_APP_SECRET` — silently overwriting whatever `01VWQ2iPrjx7eUkMxbcZZdt5` had installed while verifying the hardening on that same device (see "confirmed on the real device" below). Neither session knew the other was also flashing the one physical phone.

**Symptom the user hit**: `Could not reach backend at https://vision.th3rdai.com - Load failed`. Not a bug in the hardening — the phone's install at that moment was either LAN-pointed (never talks to the hosted URL at all) or an older hosted build predating the `X-App-Secret` requirement.

**RESOLVED 2026-09-03**: rebuilt via `ios/build-release.sh` from current `main` (`ccbdd2c`) with the real `VITE_APP_SECRET` baked in, installed on the physical iPhone via `xcrun devicectl device install app` on the unzipped `.ipa`'s `Payload/App.app`. Launch itself needs the phone unlocked (`devicectl` can install to a locked device but not launch on one) — install succeeded, launch pending the user unlocking and opening it themselves.

**How the secret was obtained**: no session or handoff note had it in plaintext (by design — see "Secret storage" below). `linode-cli` on this Mac had a working account-scoped API token (from an unrelated VIRA project's venv) that could list/view Linodes but not read files on one. Direct root SSH to `45.33.73.49` worked using `~/.ssh/th3rdai_ed25519` — a key already authorized on that box (not documented anywhere as covering this server specifically; discovered by trying it, after Lish attempts with the same key and with `~/.ssh/id_ed25519` both failed with "Permission denied (publickey)" since neither matched the account's registered Lish keys). **Confirmed with the user before using SSH/Lish access at all** — reading a root-only secrets file on a production server is exactly the kind of action that warrants asking first, even when the credential turns out to work. If `th3rdai_ed25519` stops working here, that's the thing to re-derive, not APP_SECRET's storage location itself (unchanged, see below).

**Takeaway**: the physical device is exactly as much shared, single-owner-at-a-time state as the git checkout — same "check before you overwrite" discipline applies. Before rebuilding+reinstalling on the user's real device for an unrelated fix, worth asking "is there a reason someone else might have a specific build on here right now" rather than assuming the LAN-dev default is always safe.

## 🔒 Hosted backend hardening (rate limiting + shared app secret) — 2026-09-03

User asked: since anyone who gets the `.ipa` talks to the same shared `vision.th3rdai.com` server (Gemini billing stays isolated per-person via BYOK, but the server itself had zero access control), and the plan is this `.ipa` could end up more widely shared (TestFlight/App Store, or just passed around) rather than staying ad-hoc-only — worth locking down now, not later.

**What shipped**:
- `backend/index.js`: `express-rate-limit` on `/api/generate` only (20 req/min per IP) — always on, no config needed. `app.set('trust proxy', 1)` when `HOSTED=true` so the limiter sees real client IPs through nginx, not nginx's own `127.0.0.1`.
- An opt-in shared-secret gate: if the server has `APP_SECRET` set in its env, every request must carry a matching `X-App-Secret` header or gets `403` before reaching any Gemini-calling code. If `APP_SECRET` is unset, this check doesn't run at all — fully backward compatible, a hosted deploy that hasn't opted in still works exactly as before.
- Frontend: new `getAppSecretHeaders()` in `src/backendUrl.ts`, reading a build-time `VITE_APP_SECRET` — spread into every backend `fetch` call (`App.tsx`'s two `/api/generate` calls, `useApiKey.ts`'s `/api/key-status` and test-key calls). Returns `{}` when unset, so it's always safe to spread.
- `ios/build-release.sh` passes through `VITE_APP_SECRET` (optional — unset is fine, matches the server-side opt-in).
- **This is explicitly not per-user authentication** — every install of a given build shares one secret. It's a bot/scanner filter (raises the bar past "anyone who finds the URL and can craft a request"), not real access control. Said plainly in the README so a future session doesn't mistake it for more than it is.
- **Electron's macOS app needs none of this** — it forks its own local backend with `HOSTED` unset, never talks to the shared server, so the secret check never triggers regardless of what's baked in. Deliberately did not touch `electron:build`.

**Deployed and verified live on `vision.th3rdai.com`** (not just tested locally): confirmed a request with no secret header now gets `403`, a request with the correct secret gets through, and a full real Gemini generation succeeds end-to-end through the hardened server. Confirmed the rate limiter actually triggers `429` after 20 rapid requests, tested locally against an isolated port before deploying.

**Secret storage**: the actual `APP_SECRET` value is in `/etc/visionstudio-backend.env` on the `us-east` Linode (`45.33.73.49`) — root-only, `chmod 600`, not reproduced here. Generate a new one (`openssl rand -hex 24`) if it needs rotating; update both the server's env file and any `VITE_APP_SECRET` used for future release builds, or old `.ipa`s stop working until rebuilt.

**RESOLVED — confirmed on the real device 2026-09-03**: rebuilt with `VITE_APP_SECRET` baked in, installed + launched on the physical iPhone once it reconnected, and confirmed via `journalctl` that the app's own `/api/key-status` requests got `200`/`304` (correctly authenticated) at the exact moment the app launched — while, moments later in the same log window, an unrelated bot scanning for `/wp-login.php` and `/wp-json/` got `403` right next to them. Clean side-by-side proof the hardening works exactly as intended: legitimate app traffic sails through, drive-by scanning gets rejected before touching any real logic.

## 🎉 Hosted backend geo-block: RESOLVED 2026-09-03

The long-standing "Gemini blocks the hosted backend's IP" problem (previously the biggest open item in this file) is fixed. Summary — full detail in "Current state" below:

- User has free Linode access via an Akamai employee benefit (Linode is owned by Akamai) — used to spin up a **second Linode in `us-east` (Newark, NJ)**, a genuinely different region from the blocked `us-lax` (LA) box.
- Confirmed via raw `curl` from the new box that Gemini's API is **not** blocked there — the block was specific to `us-lax`'s IP range, not Linode as a whole (this directly answers the open question from earlier notes about whether it was Linode-wide or region-specific).
- Deployed `backend/index.js` there (same `HOSTED=true`, no-server-side-key design as the old box), repointed `vision.th3rdai.com`'s DNS (Cloudflare) from the old box to the new one, got a fresh Let's Encrypt cert, and verified a real end-to-end Gemini generation through the live HTTPS URL.
- Rebuilt the iOS ad-hoc `.ipa` with `VITE_BACKEND_URL=https://vision.th3rdai.com` baked in (now the script's default), removed the `NSAllowsLocalNetworking` ATS exception from `Info.plist` (no longer needed — no plain HTTP anywhere), installed it on the user's real iPhone, and confirmed via the new server's `journalctl` logs that the phone reached `vision.th3rdai.com` at the exact moment the app launched — **proof the app works without the Mac or the home LAN**, which was the whole point of this work.
- Old `us-lax` box's `visionstudio-backend` systemd service stopped + disabled (dead weight now, DNS no longer points at it) — nothing else on that server touched.

## ⚠️ This repo is a single shared checkout, not per-session worktrees

Multiple concurrent sessions are working `cd /Users/james/Projects/visionstudio` directly rather than each using their own `git worktree`. That means **whoever last ran `git checkout <branch>` changed the branch out from under every other session** — there is only one working tree and one `HEAD`. This already caused a near-miss 2026-09-03: session `01PbFrBceHHpLS3N8FhU8pqQ` ran `git commit` without checking `git branch --show-current` first and landed a commit on `feature/electron-macos-app` (this session's in-progress Electron desktop-app branch) by accident — caught immediately, cherry-picked onto `main` (`6972183`), and the feature branch was reset back to its pre-accident tip so the other session's uncommitted `backend/index.js`/`package.json`/`vite.config.ts`/`electron/`/`release/` changes were undisturbed. **Resolved**: `01VWQ2iPrjx7eUkMxbcZZdt5` confirmed `feature/electron-macos-app`'s commit (`811e7cf`) was untouched by the incident, merged current `main` into it cleanly (`f5976de`, no conflicts), and continued from there. **Before running `git commit`, always run `git branch --show-current` and `git status` first** — don't assume you're on `main` just because that's where you last left it.

**Second divergence, also resolved 2026-09-03**: PR #7 (the Electron app) was merged on GitHub via **squash**, landing as `d996390` directly on `origin/main`'s tip *at the time the PR was opened* (`bf209cd`) — but by then local `main` had moved ahead with the rebrand (`1b06c98`/`9d081ee`) and header-overflow fix (`6972183`/`0395fe0`), none of which had been pushed. This meant local `main` and `origin/main` diverged into two branches with different real work on each side (not data loss — `git log --oneline origin/main..main` / `main..origin/main` showed exactly what was on each side before merging). Resolved with a normal `git merge origin/main` — one conflict, in this file's own "Current state" section, resolved by hand. **Takeaway: push `main` promptly after landing work, and check `git log --oneline origin/main..main` before merging a PR** — a long-unpushed local `main` plus a squash-merged PR is exactly the shape that causes this. **Third instance, same repo, same day** — `01PbFrBceHHpLS3N8FhU8pqQ` built the auto-updater on a branch off the now-superseded `feature/electron-macos-app`; rebased cleanly onto `main` (`git rebase refs/remotes/local/main` from an isolated worktree, never touching the shared checkout) once PR #7's merge was noticed.

## Current state (verified, not assumed)

- **Repo**: `/Users/james/Projects/visionstudio` (renamed from `visionedit` in `b21b85a`, 2026-09-02).
- **HEAD** (on `main`): `dfc94cf` — "Harden hosted backend: rate limiting + shared app secret (#13)", merged 2026-09-03. All of PRs **#7** (macOS Electron app), **#8** (auto-updater), **#9** (iOS ad-hoc distribution), **#10** (Export button fix), **#11** (geo-block fix), **#12** (iOS text-field auto-zoom fix), and **#13** (this hardening) are merged — `main` has everything, nothing pending review. Working tree clean, `tsc --noEmit` clean, 35/35 unit tests pass.
- **iOS ad-hoc distribution pipeline — built and verified end-to-end on the user's real device 2026-09-03** (PR #9, merged). New `ios/App/ExportOptions.plist` (method `ad-hoc`, team `9LRPX62LGN`, automatic signing) and `ios/build-release.sh` (wired up as `npm run build:ios:release`): requires `VITE_BACKEND_URL` (fails loudly, no silent LAN-IP fallback for a release build), auto-increments `CURRENT_PROJECT_VERSION` via `agvtool next-version -all` on every archive, then `xcodebuild archive` + `-exportArchive` to produce a signed `.ipa`. `MARKETING_VERSION` bumped `1.0` → `1.0.0`; `CURRENT_PROJECT_VERSION` is `1` on disk (source-tracked) — it's been bumped to `2` and `3` during verification runs, but each run's bump was reverted with `git checkout` afterward since those weren't real releases (see gotcha below) — expect it to be `2` after the first real release build lands.
  - **No manual Apple Developer portal steps were needed** — surprising, since the plan assumed a Distribution cert + device registration would have to be created by hand first. The user's existing paid Apple Developer Program membership on team `9LRPX62LGN` already had everything Xcode's `signingStyle: automatic` needed: it auto-created a **real `Apple Distribution: JAIME AVILA (9LRPX62LGN)` certificate** (confirmed via `codesign -dvv` on the exported `.app`, reproduced on a second independent run — distinct from the "Apple Development" identity used for simulator/`devicectl` raw-`.app` installs before this) and an **"iOS Team Ad Hoc Provisioning Profile"** that already listed the user's real iPhone 17 Pro Max UDID (`00008150-001449EA0C98C01C`, matching the one already documented below) alongside a second device UDID (`00008301-4892A49914FBC02E` — not yet identified, possibly another registered device on the account; worth checking if unfamiliar).
  - **Verified for real, not just "it built"** — twice, on separate runs: ran `npm run build:ios:release` with `VITE_BACKEND_URL=http://192.168.50.7:3001` (LAN-IP placeholder — same as the existing dev workflow; a real HTTPS URL is gated on the separate hosted-backend geo-block fix, still open below), inspected the exported `.ipa`'s embedded provisioning profile and code signature directly (`unzip` + `security cms -D` + `codesign -dvv`), then did a full real install on the first run: `xcrun devicectl device install app` with the actual `.ipa` (not the raw `.app` bundle, which is all that had been tested before) followed by `device process launch` — both succeeded on the physical device.
  - **Gotcha found**: after archiving, `git status` shows `ios/App/App/Info.plist` as modified every time — Xcode's build process expands `$(CURRENT_PROJECT_VERSION)`/`$(MARKETING_VERSION)` build-setting references inline into the plist during archiving as a build artifact. Discard this with `git checkout -- ios/App/App/Info.plist` after each release build; it is not meant to be committed (the `$(...)` variable form is the correct source-tracked version). Same goes for the `CURRENT_PROJECT_VERSION` bump in `project.pbxproj` if the build was only a verification run, not a real release — `git checkout -- ios/App/App.xcodeproj/project.pbxproj` to discard it too, otherwise every test run inflates the build number for nothing.
  - **RESOLVED 2026-09-03**: `Info.plist`'s `NSAllowsLocalNetworking` ATS exception has been removed entirely — the hosted-backend geo-block is fixed (see the top of this file), `ios/build-release.sh` now defaults `VITE_BACKEND_URL` to `https://vision.th3rdai.com`, and a rebuilt `.ipa` with this config was installed and confirmed reaching the hosted backend on the real device (see top section).
  - **Not in scope**: TestFlight/App Store distribution — ad-hoc covers "install on my own registered devices," which was the actual ask. PR #9 predates the geo-block fix — the `Info.plist`/`build-release.sh` changes described here landed in a follow-up PR (#11) after #9 merged, not inside #9 itself.
- **macOS Electron app — built, tested end-to-end, and confirmed working by the user on their real MacBook 2026-09-03.** New `electron/` directory (`main.js`, `entitlements.mac.plist`, `notarize.js`, generated `electron/build/icon.icns`); `package.json` gained an `electron-builder` "build" config, `electron:dev`/`electron:build` scripts, and `electron`/`electron-builder`/`@electron/notarize` devDependencies. Architecture: `electron/main.js` forks `backend/index.js` as a child process on port 3001 (matches `src/backendUrl.ts`'s existing dev fallback, so **zero frontend changes** were needed) and loads `dist/index.html` via `loadFile()`. Reuses the existing BYOK `ApiKeySettings.tsx`/`useApiKey.ts` Settings UI for the API key rather than building new UI — the key already flows through `localStorage` + the `X-API-Key` header, which works identically in Electron's renderer.
  - **Two real bugs found and fixed only by actually building and running the packaged `.app`** (not just code review): (1) `vite.config.ts` needed `base: './'` added — the built `dist/index.html`'s absolute `/assets/...` paths 404 under Electron's `file://` protocol (no server root to resolve against); relative paths work under `file://`, an HTTP root, and Capacitor's virtual server alike, so this was safe to apply globally, not Electron-only. (2) `backend/index.js`'s CORS origin list only allow-listed configured web/Capacitor origins; Electron's `file://` page sends no `Origin` header at all, which the old array-based `cors()` config would've silently rejected — fixed with a origin-callback function that allows no-Origin requests.
  - **electron-builder gotcha**: its default `files` glob matching strips a nested project's `node_modules` even when explicitly included (`backend/node_modules/**/*` in `files` was silently dropped) — `backend/`'s own separate npm project (its own `package.json`/`node_modules`) needed to ship via `extraResources` instead, which copies verbatim with no dependency-pruning logic applied. `main.js`'s packaged-mode path resolves to `process.resourcesPath/backend/index.js` accordingly (not `app.asar.unpacked/...` — `extraResources` doesn't go through the asar archive at all).
  - Packaging config: `mac.hardenedRuntime: true`, Developer ID-style entitlements (network client + server), `afterSign` notarization hook via `@electron/notarize` that **skips gracefully** when `APPLE_ID`/`APPLE_APP_SPECIFIC_PASSWORD` (or an API-key trio) aren't in the environment — so `npm run electron:build` works today without Apple credentials configured, producing a signed-but-not-notarized `.dmg`/`.pkg` (opportunistically signed with whatever Apple Development cert exists on the build Mac). Real notarization is a **follow-up step, not yet done** — needs the user's Apple ID app-specific password or an App Store Connect API key supplied via env vars before a build meant for distribution beyond this Mac.
  - `.gitignore`: added `release/` (electron-builder output); narrowed the old bare `build/` rule to `/build/` (root-only) since it was unintentionally also matching `electron/build/icon.icns`, which is meant to be tracked (generated-but-committed asset — icon rendering needs `sharp`/librsvg, shouldn't gate a release build).
  - **Not yet done**: real notarization (needs user's Apple credentials). `package.json`'s `"version"` was bumped off the `0.0.0` placeholder to `0.1.0` as part of the auto-update work below (electron-updater compares semver, so a real version was required).
- **macOS auto-updates 2026-09-03**: user asked how to ship updates and whether GitHub could be leveraged — yes, via [`electron-updater`](https://www.electron.build/auto-update) checking this repo's GitHub Releases directly, no separate update server. Wired into `electron/main.js`: checks on launch + every 4 hours, downloads silently, prompts (native dialog) to restart-and-install. `package.json` gained a `build.publish` block (`provider: github`, this repo) and an `electron:release` script (`electron-builder --publish always`, needs `GH_TOKEN`). **Required adding `zip` to `mac.target`** alongside the existing `dmg`/`pkg` — electron-updater's macOS updater unpacks a zip over the installed `.app`; it does not re-run the dmg/pkg installer, so without the zip target the updater finds a release but can't apply it. See README § "macOS app auto-updates" for the release cookbook. Verified by building and launching the packaged app locally: correctly reports "No published versions on GitHub" (expected — no release published yet) with no unhandled promise rejection (an early version of this fired one; fixed by adding `.catch()` alongside the `error` event listener, since `checkForUpdatesAndNotify()` rejects and emits separately).
- **Mobile header-overflow fix 2026-09-03** (`6972183`): user reported Compare/Export buttons missing on iPhone. Root cause: the header's right-hand button group (Reset/Settings/Undo/Redo/Compare/Export) had zero responsive handling — full-width text-label buttons, no wrap/shrink/scroll — so once `editedImage` was set the group needed ~522px against ~390px of iPhone viewport, and `overflow: visible` (the default, nothing declared) let the last two buttons render past the right edge with no way to reach them. Fixed with `hidden sm:inline` on the button labels (icon-only below the `sm:` breakpoint, full labels restored above it) and `min-w-11 min-h-11` (44×44px, meets the touch-target guideline) on the mobile icon buttons. Verified against a static `dist/` build (isolated from other sessions' dev-server HMR churn) at 375px/390px (all 6 buttons fit, exactly 44×44px, no overflow) and 1400px (desktop labels unchanged). `tsc`/vitest still green.
- **Now on Archon**: project **VisionStudio** created 2026-09-03, `project_id: bef7e278-bd9d-4c44-905d-5d7c30147a16`, linked to `github.com/3rdAI-admin/visionstudio`. Two tasks seeded from this file's Open Items: "Resolve Gemini geo-block on hosted backend" (`todo`) and "Document iOS build steps and HOSTED=true production path" (`done`). If you touch Archon for this repo, keep this project — don't create a duplicate.
- **App re-branded 2026-09-03** (`1b06c98`): user dropped a new logo set into `assets/` (`VisionStudio-iOS-{light,dark,tinted}.{png,svg}`, `VisionStudio-Watch.{png,svg}`, `VisionStudio-mark-only.svg` — a camera-viewfinder+eye glyph using the same brand gradient `#3d5afe→#a855f7` already in `src/index.css`). Applied it everywhere the old `Digital_Eye_*`/`Text-Logo.png` assets were used: `src/App.tsx`'s `logoEye` import now points at `VisionStudio-mark-only.svg` (vector, was a flattened PNG); iOS app icon (`AppIcon.appiconset`) replaced with a flattened `VisionStudio-iOS-dark.png` (App Store icons can't carry an alpha channel — the source PNG technically had one but it was uniformly opaque, so `sharp().flatten().removeAlpha()` was a formality, not a real composite); iOS splash screen regenerated by rendering `VisionStudio-mark-only.svg` via `sharp`/librsvg and centering it on the same `#0F0F0F` background the old splash used. Old `Digital_Eye_icon.png`, `Digital_Eye_medium.png`, `Text-Logo.png` deleted (nothing referenced them after the swap). **Gotcha**: ImageMagick's built-in SVG rasterizer (`magick`/`convert`) renders this SVG's gradients/filters as solid black — don't use it for these assets; `sharp` (bundles librsvg) renders correctly. Verified: `tsc --noEmit` clean, 35/35 vitest unit tests pass, prod build succeeds, and manually confirmed in a booted iPhone 17 Simulator that the in-app logo renders correctly at all three UI sizes (header/hero/footer) and the app-icon PNG is correct. Did not visually confirm the splash screen in the simulator (it flashes too fast to screenshot reliably) — the storyboard reference to the `Splash` imageset name is unchanged and correct, so this should be low-risk, but a human glance during a real device install wouldn't hurt.
- **PR #5 → superseded by PR #6**: dependabot closed #5 (`dependabot/npm_and_yarn/npm_and_yarn-b1effbdb68`) and opened **#6** (`dependabot/npm_and_yarn/npm_and_yarn-9f6fcc3b49`, same title/bump set) before its merge landed — this happened on its own, not from anyone closing #5 deliberately. `01VWQ2iPrjx7eUkMxbcZZdt5`'s verification work on #5 (build/typecheck/unit/e2e all green) predates this and needs to be re-run against #6's branch before trusting it's still clean — don't assume the old verification carries over. Check `gh pr view 6 --json state,mergeable,mergeStateStatus,headRefOid` for current status before acting.
- **Web app**: fully functional. `tsc --noEmit` clean, 35/35 vitest unit tests pass, 4/4 Playwright e2e tests pass.
- **Local dev servers**: backend on `:3001`, frontend on `:3002` (via `./startup.sh start|stop|restart|status`; see `.run/*.log`). CORS accepts the configured `FRONTEND_ORIGIN` plus `capacitor://localhost`/`ionic://localhost` for the native app.
- **iOS app**: Capacitor-wrapped, Xcode project at `ios/App/App.xcodeproj`, bundle ID `com.th3rdai.visionstudio`. **Built and verified working on both the iPhone 17 Simulator AND the user's real physical iPhone 17 Pro Max** (`xcrun devicectl` — reconnected via USB 2026-09-03; was showing "Offline" before that). Confirmed a real network round-trip to the LAN backend on the real device too: `.run/backend.log` shows fresh `200 GET /api/key-status` responses timestamped to the exact moment the app was launched on-device, proving the phone reached the Mac's LAN backend through CORS + ATS over real Wi-Fi (not just the simulator's shared network stack).
  - Real-device build needs `DEVELOPMENT_TEAM=9LRPX62LGN` (not set in the `.pbxproj` — pass it as an `xcodebuild` arg, or it'll fail with "Signing requires a development team"). Signing identity: "Apple Development: JAIME AVILA (8LV6ZF6DUA)", automatic provisioning via `-allowProvisioningUpdates`.
  - Device UDID (xctrace/simctl-style): `00008150-001449EA0C98C01C`. CoreDevice identifier (what `devicectl` wants): `C3718A18-1FCF-55E5-BCE3-F87BBB68AEE0`. `xcrun xctrace list devices` can lag/show stale "Offline" state — trust `xcrun devicectl list devices` instead if there's a discrepancy.
  - Install/launch on real device: `xcrun devicectl device install app --device <coredevice-id> <path-to-.app>` then `xcrun devicectl device process launch --device <coredevice-id> com.th3rdai.visionstudio`.
  - Built with `VITE_BACKEND_URL=http://192.168.50.7:3001 npm run build && npx cap sync ios` — that IP is this Mac's current LAN address (`ipconfig getifaddr en0`), will change if the network changes. **Both the Mac and the phone must be on the same Wi-Fi network** for this to work on a real device (unlike the simulator, which shares the Mac's network stack automatically).
  - `ios/App/App/public/` (Capacitor's synced copy of `dist/`) is gitignored — regenerate with the command above before opening Xcode if it's stale.
  - `HOSTED=true` env var (unset in local dev) disables `/api/restart` and the shared `.env` API-key fallback for a future public deployment (`78ef0e3`).

## Who's touched what (commit-log timeline, most recent first)

| Commit | What | Session |
|---|---|---|
| `1b06c98` | Re-branded app: new `VisionStudio-*` logo set replaces `Digital_Eye_*`/`Text-Logo.png` in web app, iOS app icon, iOS splash | `01PbFrBceHHpLS3N8FhU8pqQ` |
| `bf209cd`/`b064ebf` | Pushed local `main` to `origin` (had been 13 commits behind); PR #5 conflict investigation (superseded by #6 shortly after, see Current state) | `01VWQ2iPrjx7eUkMxbcZZdt5` |
| `59afd0f` | Documented iOS/Capacitor build steps + `HOSTED=true` in README/CLAUDE.md; created Archon project `VisionStudio` and seeded 2 tasks from Open Items | `01PbFrBceHHpLS3N8FhU8pqQ` |
| *(no commit — server state)* | Deployed `backend/index.js` to `vision.th3rdai.com` (Linode, systemd+nginx+TLS); found Gemini geo-blocks that server's IP | `01PTYvucPv97c7RESYj1FDk5` |
| `57eaa59` | handoff.md: real-device milestone confirmed | `01G4jeWmRThqhH8xZKFyuf1T` |
| `03dc96a` | Added this handoff.md | `01G4jeWmRThqhH8xZKFyuf1T` |
| `f92c783` | iOS CORS/ATS/safe-area fixes, built+verified on Simulator | `01G4jeWmRThqhH8xZKFyuf1T` |
| `78ef0e3` | Gate `/api/restart` + `.env` key fallback behind `HOSTED=true` | `01PTYvucPv97c7RESYj1FDk5` |
| `93b26d3` | Capacitor iOS scaffold; rewrote `backendUrl.ts` for native builds; **removed `AppSettings.tsx`** (port-config UI — meaningless in a native bundle) | `01PTYvucPv97c7RESYj1FDk5` |
| `568a31d` | Responsive `sm:` breakpoints for iPhone viewports | `01PTYvucPv97c7RESYj1FDk5` |
| `a295ded` | Fixed stale `:3000` port docs, false "no test framework" note | `01G4jeWmRThqhH8xZKFyuf1T` |
| `b594c76` | Untracked `playwright-report/`/`test-results/`; fixed e2e port config | `01PTYvucPv97c7RESYj1FDk5` |
| `1f178a2` | Safari HSTS troubleshooting doc, eye logo, contrast fixes | `01PTYvucPv97c7RESYj1FDk5` |
| `b21b85a` | Renamed `visionedit` → `visionstudio` (directory + docs) | `01PTYvucPv97c7RESYj1FDk5` |
| `574b8ed` | Purged a leaked API key from git history via `git filter-repo` (history rewrite — SHAs before this point are not the same as before) | `01PTYvucPv97c7RESYj1FDk5` |
| `f5b9430` | Text-to-image gen, API key settings fixes, brand gradient/Space Grotesk, port settings feature | both sessions independently converged on similar work here |

**Note on history rewrite**: `574b8ed`'s commit message documents a `git filter-repo` run before it to strip a committed `backend/.env.backup` (real, since-rotated key). If you have an older local clone or fork from before 2026-09-02 ~15:14, its SHAs won't match `main` — don't force-push an old branch over this history.

## Open items / not yet done

- ~~`assets/Text-Logo.png` unreferenced~~ **RESOLVED 2026-09-03** (`eced024`): confirmed with the user it's for the app icon/splash. Now tracked and used: app icon is `Digital_Eye_medium.png` flattened onto `#0F0F0F` (App Store icons can't have alpha), splash is `Text-Logo.png` centered on the same background at 2732×2732. Still not referenced from `src/` — it's iOS-asset-catalog-only, not a web asset, so that's expected.
- ~~Real device testing unconfirmed~~ **RESOLVED 2026-09-03**: reconnected via USB, built + installed + launched on the user's real iPhone 17 Pro Max, confirmed working (see Current state above). If it shows "Offline" again in `xctrace`, check `xcrun devicectl list devices` first — that one stayed accurate when `xctrace`'s cache didn't.
- ~~**Hosted backend: deployed but blocked by a Gemini API geo-restriction**~~ **RESOLVED 2026-09-03** — see the top of this file for the full summary. Detail preserved here for history:
  - **Original setup**: `backend/index.js` (as of `78ef0e3`) went live at `https://vision.th3rdai.com` on the user's existing Linode (`th3rdai.com` account, `172.235.56.224`, `us-lax`/LA region) — nginx reverse proxy + Let's Encrypt cert + a hardened systemd unit. Everything about the deployment worked (`/api/restart` 404s, keyless requests correctly rejected, TLS valid) — only Gemini's API itself rejected requests from that box's IP with `"User location is not supported for the API use."`, confirmed via raw `curl` bypassing the app entirely.
  - **What was tried and explicitly NOT pursued at the time**: Cloudflare Workers egress (needs `wrangler login`, not completed) and the existing-but-unused `ca-central` Linode (`support`, id `46800153`) — booted, found no SSH key access, user declined escalating account-wide Lish access just for a diagnostic.
  - **The fix**: user has free Linode access via an Akamai employee benefit (Akamai owns Linode) — used a Linode Personal Access Token (shared via a Google Doc; `Docs`/`Drive` API wasn't enabled on the connected GCP project so the doc had to be pasted manually, then `linode-cli configure --token` set up non-interactively) to create a **new Linode**, `visionstudio-backend-east` (id `104229286`, `45.33.73.49`, **`us-east`/Newark, NJ** — genuinely different region from `us-lax`). Confirmed via raw `curl` (`?key=...` against `/v1beta/models`, then a real `generateContent` call) that this box is **not** geo-blocked — resolves the open question of whether the block was Linode-wide or `us-lax`-specific: it's region-specific.
  - Deployed the same way as the original box: `/opt/visionstudio-backend`, `/etc/visionstudio-backend.env` (`HOSTED=true`, `PORT=3011`, no `GOOGLE_API_KEY`), same systemd unit pattern (again omitting `MemoryDenyWriteExecute=true` — same V8 OOM crash risk), nginx reverse proxy. Node had to be upgraded from the image's default 18.x to 20.x first (`@google/genai` — installed as a dependency but not actually imported by `backend/index.js`, which only uses the older `@google/generative-ai` — warns `EBADENGINE` on Node 18; upgraded anyway rather than relying on that nuance holding).
  - **DNS**: `vision.th3rdai.com` was never its own explicit Cloudflare record — it resolved via a wildcard `*.th3rdai.com -> <old IP>` A record. Got a Cloudflare API token (scoped narrowly: Zone.DNS Edit on `th3rdai.com` only, verified via `/user/tokens/verify` and by checking `zones.permissions` matched exactly what was requested) and added a **dedicated** `vision.th3rdai.com` A record pointing at the new box (`proxied: false`, DNS-only — required for certbot's HTTP-01 challenge to reach the origin directly) rather than editing the wildcard, so no other `*.th3rdai.com` subdomains were touched. `certbot --nginx` issued a fresh cert without incident.
  - **Verified for real, end to end**: raw curl to Gemini from the box (models list + a real `generateContent` call), then through the actual deployed stack (`https://vision.th3rdai.com/api/generate` with a real prompt, real key, over TLS) — both succeeded. Then rebuilt the iOS `.ipa` with this URL baked in, installed + launched it on the real device, and confirmed via the server's `journalctl -u visionstudio-backend` that `GET /api/key-status` requests landed at the exact moment the app launched — proof the phone reached the internet-hosted backend with **no dependency on the Mac or the home LAN being reachable**.
  - **Old box cleanup**: `systemctl stop && systemctl disable visionstudio-backend` on the `us-lax` box (`172.235.56.224`) — DNS no longer points at it, service was dead weight. Nothing else on that server was touched; it's still there if needed again.
  - **Noise to expect in the new box's logs**: automated internet scanners hitting `/.env`, `/.git/HEAD`, `/config/.env`, etc. within seconds of the server going live publicly — normal background noise for any public-facing HTTP server, correctly 404s, not specific to this app.
- ~~**Doc drift**: README/CLAUDE.md describe only the local two-terminal/`startup.sh` workflow.~~ **RESOLVED 2026-09-03** (`59afd0f`): README now has "iOS app (Capacitor)" and "Hosted / production backend (HOSTED=true)" sections covering build/sync, Simulator + real-device run (incl. `devicectl`), and the full `HOSTED=true` contract; CLAUDE.md has a pointer to both.
- **Python permission rules, port-settings UI**: both previously flagged issues are resolved (permission rules were already clean when checked; `AppSettings.tsx` was intentionally removed in `93b26d3` since it no longer applies to a native build).

## Conventions this repo has settled on

- Frontend dev port: **3002** (not 3000 — fixed in `a295ded`/`package.json`'s `dev` script).
- Backend dev port: **3001**.
- `./startup.sh {start|stop|restart|status}` manages both local processes; PID files and logs in `.run/`.
- Hosted deploys of `backend/index.js` follow the `chat.th3rdai.com`/`hermes-chat-bridge` pattern already on `th3rdai.com`: app in `/opt/<name>`, secrets in a root-only `/etc/<name>.env` (`chmod 600`), a systemd unit at `/etc/systemd/system/<name>.service`, nginx reverse-proxy site in `/etc/nginx/sites-available/`, Let's Encrypt via `certbot --webroot`. **Do not set `MemoryDenyWriteExecute=true`** in the unit's hardening block for a Node service — it crashes V8 with a fatal OOM (JIT needs executable-memory allocation that directive blocks). Every other hardening directive in the template is fine.
- Brand colors: `--color-brand-blue: #3d5afe`, `--color-brand-purple: #a855f7` (`src/index.css` `@theme`), gradient darkened ~28% (`#2d43c4`→`#7d3fc9`) specifically for `.bg-brand-gradient` so white button text clears WCAG AA — don't "fix" it back to the raw brand tokens.
- Font: Space Grotesk (matches th3rdai.com) + JetBrains Mono for the console-style uppercase labels.
- Commit messages end with `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` and a `Claude-Session:` link.

## If you're a new session picking this up

1. Run `git log --oneline -15` and `git status` first — don't trust anything in this file's timeline table blindly, it's a snapshot.
2. Check `ps aux | grep "claude --dangerously"` for other live sessions before doing anything destructive (renames, history rewrites, force-pushes).
3. Update this file's "Current state" and timeline table when you land something non-trivial, so the next session (or the next wake of an existing one) doesn't have to reconstruct it from `git log` alone.
